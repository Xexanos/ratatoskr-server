# FOR LOCAL DEVELOPMENT ONLY. `pnpm dev` and `pnpm --filter @ratatoskr/app start` load this file
# via Node's --env-file. Copy it to .env and fill in your values.
#
# Deploying with Docker? You do NOT need this file — set the variables directly in the
# `environment:` block of compose.yaml, which documents every option.

# Required — your Audiobookshelf server.
ABS_URL=https://192.168.1.50:13378
# Required — API key of a dedicated, low-privilege, stream-only ABS account (see the README).
ABS_STREAMER_API_KEY=

# Required — the encrypted store of signed-in devices (SPEC section 8), and the key that encrypts
# it. Generate the key with `openssl rand -base64 32`; it has to be a random 256-bit value, not a
# passphrase you make up (SPEC section 7 says why).
#
# Rather not keep a random string around? Use SESSION_STORE_KEY_FILE instead — point it at a file
# holding the key and let a password manager or `docker secret` own that file, so you never store or
# retype the value yourself. Set exactly one of the two; both together is a config error.
#SESSION_STORE_KEY_FILE=../../.local/session-store.key
SESSION_STORE_KEY=
#
# Unlike the container, `pnpm dev` picks no location for the store and creates no directory: run
# `mkdir -p .local` in the repo root once (it is gitignored). The path below is relative to
# `packages/app`, which is where the dev and start scripts run — hence the `../../`. An absolute path
# works too, and any directory outside a package's source tree will do.
SESSION_STORE_PATH=../../.local/sessions.enc

# Ratatoskr's own listener. Locally the simplest option is plain HTTP; for HTTPS set
# TLS_CERT_PATH/TLS_KEY_PATH instead. (Unlike the container, `pnpm dev` does not auto-generate a
# certificate — that convenience lives only in the Docker entrypoint.)
ALLOW_PLAIN_HTTP=true

# Any other variable the server understands can be set here too. The full list, with defaults and
# explanations, lives in compose.yaml; SPEC sections 7 and 14 carry the deeper rationale. Examples:
#SONOS_SEED_HOST=192.168.1.60
#POLL_INTERVAL_SECONDS=15
